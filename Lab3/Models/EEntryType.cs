namespace Lab3.Models
{
    /// <summary>
    /// Тип элемента ПОЛИЗ: etCmd (команда), etVar (переменная), etConst (константа), etCmdPtr (адрес).
    /// </summary>
    public enum EEntryType
    {
        etCmd,
        etVar,
        etConst,
        etCmdPtr
    }
}
